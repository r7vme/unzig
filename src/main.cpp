#include <cstdint>
#include <fstream>
#include <stdexcept>

struct Token {
  enum Type {
    eof,
    identifier,
    l_square,
    kw_break,
    

  }

}

int main()
{
    //    while (f.get(byte))
    //    {
    //        std::cout << byte;
    //    }
    //
    //    f.close();

    return EXIT_SUCCESS;
}
