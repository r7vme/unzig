fn foo() i32 {
    return 1;
}

fn main() i32 {
    // comment
    var x: i32 = foo(); // comment
    var y: i32 = 123 + (1 * x);
    y = 1;
    var a: bool = 0;
    var b: bool = true;

    // empty block
    {}

    var z: f32 = 1.0;
    if (1) {
      z = 1.0;
    } else if (!!z) {
      z = 2.0;
    } else {
      z = 3.0;
    }

    if (1 and 1 or 1) {}

    return 0;
}
